define(function (require) {

    var $ = require('jquery');
    var ko = require('knockout');
    var router = require('plugins/router');

    return {
        activate: function () {
            return true;
        }
    };
});